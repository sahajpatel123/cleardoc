# Daily — 2026-05-31

## Focus
Initialize the central memory system under `./memory/`.

## Done
- Built the full `./memory/` structure: `MEMORY.md`, `DECISIONS.md`, `CHANGES.md`, `TODO.md`, `RULES.md`, `KNOWLEDGE/` (README + architecture, ai-pipeline, deployment-and-schema, security, claude-md-drift), and this `daily/` folder.
- Grounded content in actual code — read `lib/ai.ts`, `lib/ai-model.ts`, `lib/free-quota.ts`, `prisma/schema.prisma`, `lib/ensure-schema.ts` diff, `package.json`, `DESIGN.md`, `AGENTS.md`; listed `lib/` + API routes; scanned for committed secrets.

## Findings (the interesting part)
- **CLAUDE.md is partly stale** → catalogued in [[KNOWLEDGE/claude-md-drift]]. Biggest one: model is **Nemotron Omni**, not llama-3.2-90b.
- **New DB columns** beyond CLAUDE.md: `User.lastResetAt`, `User.tokenVersion`, `Analysis.chatMessages/parentId/caseId`.
- **Two memory conventions coexist** — root `AGENTS.md` autonomous-agent framework uses `memory/YYYY-MM-DD.md`; this system uses `memory/daily/`. Flagged for reconciliation ([[DECISIONS]] D-002).
- **DESIGN.md conflicts with the live Atelier theme** — treated as aspirational ([[DECISIONS]] D-003).
- **Secrets:** current tree clean; git history not yet audited.
- Relocated the old stub `memory/2026-05-29.md` → `daily/daily-2026-05-29.md`.

## Session 2 — Full-site mobile responsiveness + login page polish

### Done
- **Full-site mobile responsiveness pass** across 9 pages + 3 components + globals.css.
  - Fixes: grid layout stacking, touch target sizing, reduced spacing on small screens, horizontal overflow prevention.
  - Committed (`1f3ee13`) and pushed to `main`.
- **Login page mobile improvements**:
  - Full-width submit button (was inline)
  - Larger mode-toggle touch targets (13px text, taller padding)
  - Smaller form field font size (`text-sm`) to prevent iOS auto-zoom
  - Left border → top border + horizontal ember accent on mobile
  - Tighter editorial spacing (promises list, intro text, footer)
  - "VOL. 01" footer label hidden on mobile
  - Committed (`d074123`) and pushed to `main`.
- **Health check:** `GET /api/health` returns **503** (not our code — likely Vercel cold-start or missing env).

### Findings
- Build fails in CI due to missing `OPENAI_API_KEY` (pre-existing, chat API route initializes OpenAI client at module load time — not caused by our changes).
- Vercel health endpoint returns 503 — needs investigation.

### Open / next
See [[TODO]]. Top: owner decisions on DESIGN.md and the dual memory conventions; update CLAUDE.md; audit git history for secrets.

## Session 3 — Lazarus Swarm: Full-site exhaustive audit (300 agents)

### Done
- Ran DynamicClaudeWorkflow_Emulator_v3 with 300 agents across 6 parallel cohorts + Red Team.
- **117 issues found:** 8 Critical, 18 High, 42 Medium, 49 Low.
- Categories: type safety, security, API contracts, database, AI pipeline, Stripe billing, frontend, config.
- Key critical bugs: unhandled DB errors in chat/rephrase routes, CSP `unsafe-eval`, Stripe refund/payment-failure gaps, chat limit race condition.
- Implemented fixes from prior session: sanitized login redirect, rate-limit warning, auth callback optimization, retry logic for AI, try-catch on GET routes, `invoice.payment_failed` handler, GDPR deletion endpoint.
- Fixed pre-existing broken imports (`FREE_TIER_CREDITS_PER_DAY`, `reserveFreeAnalysisCredit`, `refundFreeAnalysisCredit`).
- Fixed mobile horizontal scrolling (`overflow-x: hidden`, `overscroll-behavior-x: none`).

### Findings
- Full bug report delivered (see session output).
- Top 10 fixes prioritized by impact (~4 hours total effort).

### Open / next
- Owner decision: proceed with fixing Top 10 bugs? See [[TODO]].
